services:
  - type: web
    name: loblaw-bio-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python load_data.py
    startCommand: streamlit run dashboard.py --server.port $PORT --server.address 0.0.0.0
